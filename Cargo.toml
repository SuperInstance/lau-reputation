[package]
license = "MIT"
name = "lau-reputation"
version = "0.1.0"
edition = "2021"
description = "Reputation system for LAU — tracks how players are perceived based on actions, not words"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
